import type { User } from "blaise-api-node-client";

const mockUser: User = {
  name: "Jake Bullet",
  role: "SVT Supervisor",
  serverParks: ["gusty"],
  defaultServerPark: "gusty",
};

export default mockUser;
